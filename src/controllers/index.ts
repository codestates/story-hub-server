import oauthModule from './oauthController';
import userModule from './userController';
import commentModule from './commentController';
import boardModule from './boardController';
import commitModule from './commitController';

const controllerModule = {
  oauthModule,
  userModule,
  commentModule,
  boardModule,
  commitModule,
};

export default controllerModule;
